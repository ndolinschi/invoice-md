# Invoice MD — Freelancer invoices + PDF (complex)
Create invoices: seller/buyer, line items (desc, qty, rate MDL), VAT %, totals, status draft/sent/paid.
List + detail editor. **PDF export** via jspdf or @react-pdf/renderer (must work client-side).
Invoice number sequence. localStorage. Next.js 16 + shadcn + RO/RU.
Distinct print-ready aesthetic. Routes: `/`, `/invoice/[id]`, `/settings`. Seed 2 invoices. build=0, push ndolinschi/invoice-md.
